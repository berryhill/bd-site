---
import type { Page } from "astro";
import type { CollectionEntry } from "astro:content";
import IconArrowLeft from "@/assets/icons/IconArrowLeft.svg";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import LinkButton from "./LinkButton.astro";

export interface Props {
  page: Page<CollectionEntry<"blog">>;
}

const { page } = Astro.props;
---

{
  page.lastPage > 1 && (
    <nav class="mt-auto mb-8 flex justify-center items-center gap-4" aria-label="Pagination">
      <LinkButton
        disabled={!page.url.prev}
        href={page.url.prev as string}
        class:list={["px-6 py-3 bg-transparent hover:bg-accent text-accent hover:text-background font-bold uppercase text-sm tracking-wider transition-all duration-200 select-none", { "opacity-30 pointer-events-none": !page.url.prev }]}
        ariaLabel="Previous"
      >
        <IconArrowLeft class="inline-block rtl:rotate-180 w-4 h-4" />
        Prev
      </LinkButton>
      <span class="font-bold uppercase text-lg text-skin-base">{page.currentPage} / {page.lastPage}</span>
      <LinkButton
        disabled={!page.url.next}
        href={page.url.next as string}
        class:list={["px-6 py-3 bg-transparent hover:bg-accent text-accent hover:text-background font-bold uppercase text-sm tracking-wider transition-all duration-200 select-none", { "opacity-30 pointer-events-none": !page.url.next }]}
        ariaLabel="Next"
      >
        Next
        <IconArrowRight class="inline-block rtl:rotate-180 w-4 h-4" />
      </LinkButton>
    </nav>
  )
}
